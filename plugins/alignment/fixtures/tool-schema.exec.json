{
  "meta": {
    "kind": "tool schema structural parity",
    "source": "codex HEAD 5bc8da6d78",
    "module": "codex-rs/core/src/tools/handlers/shell_spec.rs",
    "functions": "create_exec_command_tool (15-19, default options: single environment, allow_login_shell) / create_write_stdin_tool (113-155) / create_approval_parameters (298-344)",
    "extracted": "2026-08-15",
    "note": "Property name sets, required sets and value types from the official schemas. Description text is NOT compared (DSH documents its win32/git-bash specifics). `additional_permissions` appears only when exec_permission_approvals_enabled (default false) — DSH has no permission-profile model, documented difference. `environment_id` appears only in multi-environment builds — DSH is single-environment, documented difference. DSH counterpart: tools/exec-command.js registered schemas."
  },
  "tools": {
    "exec_command": {
      "required": ["cmd"],
      "properties": {
        "cmd": "string",
        "workdir": "string",
        "tty": "boolean",
        "yield_time_ms": "number",
        "max_output_tokens": "number",
        "shell": "string",
        "login": "boolean",
        "sandbox_permissions": "string",
        "justification": "string",
        "prefix_rule": "array"
      }
    },
    "write_stdin": {
      "required": ["session_id"],
      "properties": {
        "session_id": "number",
        "chars": "string",
        "yield_time_ms": "number",
        "max_output_tokens": "number"
      }
    }
  }
}
